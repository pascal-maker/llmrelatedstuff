[project]
name = "addition-demo-v1"
version = "0.1.0"
description = "addition-demo-v1 — <one-line description>."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["addition_demo_v1"]
